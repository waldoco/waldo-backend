# First worker assignment: S0 integration and truth

Give this assignment to the first implementation agent. Product decisions are finalized in the linked plan; inspect the current source and build the first dependency frontier.

> Read [README](README.md), [WORKER_GUIDE](WORKER_GUIDE.md), and [the canonical plan](../WALDO_PERSONAL_AGENT_PRODUCT_ARCHITECTURE_AND_BUILD_PLAN_2026-09-18.md) §§2–7, 10–11 and 14. Follow each repository's current AGENTS.md. Use fresh isolated backend/app worktrees and preserve all unrelated changes. Pin the current refs; register your issue, ownership and verification through backend #116. One integration owner controls shared contracts and migrations.
>
> Start with the [bounded Brain/app/ADR synchronization](ADR_RECONCILIATION.md). The user has approved the product direction; do not start another broad market/architecture audit. Before changing an affected contradictory seam, publish its exact amendment and consumer pin. Read-only baseline work and nonconflicting containment may proceed while that synchronization is reviewed.
>
> Implement S0 only: an authenticated iPhone message reaches the existing owner Durable Object, receives one real model response and a read-only result from the user's connected test Calendar, and produces a durable, correctly ordered, owner-isolated answer. Reconnect/stop/provider failure must display the real state. Remove fictional app success and fix or disable the legacy thread-ownership defect before real-user use. Wire the actual context, spend, permission and delivery dependencies; never bypass fail-closed placeholders.
>
> Preserve Supabase transcript ownership, DO run/schedule/outbox ownership and the typed secret-owning connector proxy. Use [implementation contracts](IMPLEMENTATION_CONTRACTS.md) for staged publication and recovery. Define the smallest production interfaces needed by the next memory/health/Calendar slices. Do not add another orchestration framework, broad work-unit ceremony to ordinary chat, or begin later browser/commerce/relationship/Kennel features.
>
> Prove two-owner isolation, same-key replay and changed-byte rejection, crash/restart at publication boundaries, cancellation, late callbacks after revocation/deletion, provider failure, truthful app states and a real read-only Calendar result. Measure model cost and end-to-end latency in the actual deployment target. Local fixtures do not count as live/provider/device evidence. Hosted changes still follow their explicit authorization boundary.
>
> Run the relevant repo checks; report exact source/config pins, commands, passed/failed/not-run scenarios, sanitized evidence, migration/rollback, and unresolved gaps. Re-estimate S1/S2/H from observed S0 work. Hand off the next smallest frontier, not a claim that the whole agent is complete.

## What to return before another worker starts

- Exact owned files/interfaces and current-source gap reproduction.
- Cross-repository synchronization commits and any genuinely blocked protected seam.
- S0 implementation PR(s), evidence matching the scenarios above, and cost/latency observations.
- Released contracts for independent app/health and connector work; remaining single-writer surfaces.
- Honest next-frontier recommendation using the canonical acceptance IDs and worker completion template.

This assignment does not authorize merge, deployment, paid provider spend or unrestricted runtime actions by itself. The assigning user's implementation instruction and repository approval rules determine those boundaries.
