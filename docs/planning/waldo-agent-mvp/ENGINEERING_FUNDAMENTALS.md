# Engineering fundamentals (standing owner directive, 2026-09-24)

The owner wants senior product-engineer rigor on every slice, so a bug class shows up in our tests before it shows up in his hands. This file is the checklist. It applies at build time and at review time.

## How it is applied

1. **Before a slice ships**, walk the checklist below for every surface the slice touches. For each line that applies, point to the test that covers it, or write that test.
2. **When a bug is found** (by the owner, by a live run or by review), fix it and do both of these in the same commit:
   - add an adversarial test that reproduces the bug class, not just the one input;
   - add or sharpen a checklist line here, and add a row to the bug log at the bottom.
3. **A recurrence of a logged class is a process failure**, reported as such in the deploy report.
4. The deploy report says which checklist sections the slice touched and what is proven live versus only by tests.

## Checklist

### Tokens, auth and sessions
- A one-time token is spent only by a deliberate user action (a POST from our own page), never by a GET. Link previews, scanners and prefetchers issue GETs.
- Every URL we send in chat has previews turned off unless the preview is the point.
- Tokens have an expiry, a single use, and are compared in full. Minting a new one invalidates the old one on purpose, and the tests say so.
- Cookies: HttpOnly, Secure, a narrow Path, and a SameSite value that works for the actual navigation (Strict cookies are dropped on cross-site redirects).
- Every state-changing request checks CSRF and the owner identity. Callback data from buttons is checked against the owner before it acts.
- Secrets never reach the repo, chat, logs or traces.

- Every code-based sign-in proves in a real run that the code actually arrives: the email template must carry `{{ .Token }}`, which Supabase's default does not.

### Idempotency and retries
- Every webhook, callback, scheduled fire and migration can run twice without a second effect. Store the offset or the key before or atomically with the effect.
- Every button press is safe when pressed twice or late ("Already handled", "expired").
- Retries have a bound and a backoff. A failed step logs and leaves state that the next run can resume.
- A secret generated fresh each run but stored only when absent desyncs every other consumer on re-run. Read the stored value back and generate only when absent, so all consumers hold one value.
- An OAuth callback settles its attempt once. The state is a single-use nonce with its PKCE verifier stored server-side; a reloaded callback replays the settled outcome and never re-exchanges the single-use code. Every outcome renders a page; none throws.

### Concurrency and ordering
- Writes that belong to one turn settle before the next turn reads them.
- Anything that reads and then writes shared state runs inside the DO's serial queue or one transaction.
- External writes use a version check (etag / If-Match) so we never overwrite a change the owner made meanwhile.

### Time
- All user-facing times are in the owner's timezone. Tests cover midnight crossings, quiet hours across midnight and DST-free zones like IST.
- Expiry checks use one clock source, injected in tests.
- Anything scheduled in the past (late fire, expired proposal) has a defined outcome, and it is logged.

### Data and migrations
- Every migration takes a backup first, is idempotent, and leaves the old data in place until a later cleanup.
- Schema changes on existing Durable Object storage are additive (new table, `ADD COLUMN`) and tolerate the column already existing.
- Invariants are enforced in the database where possible (primary keys, CHECK, UNIQUE), not only in code.
- Deletes that the owner asked for are real, and anything kept afterwards (a do-not-relearn note) is disclosed.
- A commit that adds a Supabase migration updates the canonical lists (`verify-supabase-migrations.mjs` and `supabase/fixtures/assert-canonical-migration-history.sql`) in the same commit; gates.sh runs the check locally so drift fails before push.
- Attribution and audit writes name their exact row by primary key; never re-query a set ('every used invite for this email') when the row was just in hand.
- Both canonical migration lists stay in sync statically: `guard-migration-fixture-sync.mjs` compares them in gates.sh, because the SQL fixture itself only runs Mac-side and its drift is invisible in sandbox gates.

### Failure paths
- Every external call (model, Telegram, Google) has a timeout, and its failure path sends the owner something true or nothing, never a broken half-state.
- A turn that fails still records what happened in the trace.
- Model output is parsed defensively. Invalid output is rejected and logged, and it never partly applies.
- Tool output and prompt inputs have size caps.
- Gate scripts propagate failure: any failed step makes the run exit non-zero. A green-looking log line is not a pass.
- Buffers waiting on a closing event (span buffers, pending maps) have a bound and evict oldest-first.
- Structural sanitise denies (invalid shape, oversize) degrade a request to its re-sanitised essentials; only hard security denies (canary, secret, health, injection) kill a turn.

### Trust boundaries
- An edge function doing its own request auth (HMAC headers) deploys with --no-verify-jwt; probe unsigned and expect the function's own rejection, not the gateway's.
- No bearer or refresh token reaches the model, the Durable Object or the Worker. Tokens are read and used only in the connector proxy, and the runtime's database key cannot execute the token functions.
- Only the owner's own words are evidence about the owner. Files, mail, calendar and web text are data, fenced in the prompt so they cannot break its structure.
- Judgment belongs to the model. Deterministic rejects are only for hard security and safety lines.
- Raw provider passthrough fields (such as `output_items`) never enter egress-destination sanitise candidates. They are screened when replayed as input, under the internal cap.
- Connect, link and setup intent routes to connect_service or the service tool itself - a real URL from GoogleAccess.connectUrl, never model-described setup from memory. A capability with no tool gets an honest not-built answer.
- A tool's auth failure is a typed ConnectIntent on the result, delivered by the channel's offerConnect seam (rate-limited, one per service:reason per turn); the model only ever sees fixed words. External-origin tools stamp source_taint on success AND failure arms.
- Signed or secret-bearing URLs never pass through model-visible text. They travel in structured channels only (Telegram URL buttons, direct sends). Tool results must not contain them.
- An owner Durable Object never borrows the deploy owner's Telegram identity; when Telegram is unlinked, sends drop (logged) instead of rerouting.
- The rule covers every path that can mint the link, not only the connect tool: tool failure branches (not connected, missing scope) and prompts built for scheduled cards too.

### Observability
- A fallback that keeps the user surface green (e.g. single-owner routing) masks a broken integration; verify the integration path directly (edge logs, probes), not the fallback.
- Every hop logs trace id, duration, ok/failed and a short detail. The E2E checklist names the hops that prove each step.
- A live claim in a report points to a trace or a curl, not to a test.
- An exporter's own failures land where the owner can see them (the trace book), not only in a worker console.

### Database tests
- A ship report folds only EXECUTED pgTAP files into a green claim. A pgTAP file written-but-not-run is labeled UNRUN in the ship report, never counted as green; first execution is a Mac-packet step and its result is graded from raw output.
- A migration that creates a function revokes PUBLIC EXECUTE in the same file. Postgres grants EXECUTE to PUBLIC by default, which silently extends to service_role and authenticated.
- pgTAP `throws_ok` pins the 5-char SQLSTATE ('23514'), never the errname ('check_violation') - errname matching does not fire in our pgTAP build and the test goes red on a correctly-thrown error.
- `supabase test db` runs against whatever the local database currently holds; it never applies pending migrations or clears leftovers. Any runbook or CI step that runs pgTAP first rebuilds local state (`supabase db reset`, or `db start` from zero) so the tests prove the repo, not the machine.
- pgTAP files set up their own fixtures and clear any row or Vault secret they depend on, so leftovers from a live-local run cannot fail them. Live-local runs delete what they create.
- A pgTAP check reads a function's writes in a later statement. One statement sees one snapshot, so a check in the same statement sees the old rows.

### Deploy and harness config
- Remote-only bindings (Workers AI, Vectorize) live under an `env` section in wrangler.jsonc, never top-level: vitest-pool-workers starts a remote proxy for top-level bindings and the whole runtime test pool dies demanding wrangler auth.

## Bug log

| Date | Bug | Class | Test added | Checklist line |
|---|---|---|---|---|
| 2026-09-23 | Post-turn memory and spot writers raced the next turn | Concurrency | settle-before-next-turn test (W0, 96c7683) | Concurrency: writes settle before the next turn |
| 2026-09-23 | An approval could be applied after its time had passed, or over an edited event | Time, concurrency | approval expiry and etag tests (96c7683) | Time: past-scheduled outcome; Concurrency: version check |
| 2026-09-24 | Console sign-in link was spent by Telegram's link preview, so every link showed "used or expired" | Tokens | `console.test.ts` sign-in page test; token redeemed by POST only (8167bbf) | Tokens: one-time tokens spent only by a POST |
| 2026-09-24 | Console email sign-in sent Supabase's default magic-link email, which has no code to type | Tokens | `scripts/guards/guard-otp-template.mjs`; template in `supabase/templates/magic_link.html` | Tokens: code sign-in proves the code arrives |
| 2026-09-24 | A pgTAP check for Vault secret deletion read in the same statement as the revoke, so it saw the pre-delete snapshot and failed on correct code | Database tests | `supabase/tests/waldo_connections.sql` checks deletion in its own statement | Database tests: read writes in a later statement |
| 2026-09-24 | W3.2 had the DO read the refresh token back from Vault and refresh it itself, breaking the hard line that no token reaches the DO | Trust boundaries | `connections.test.ts` asserts no token in a proxy call; `waldo_connections.sql` asserts the runtime key cannot execute `proxy_secret` or `proxy_store` (W3.4) | Trust boundaries: tokens only in the connector proxy |
| 2026-09-24 | pgTAP files failed after a live-local run left an owner, presences and the router Vault secret behind | Database tests | every pgTAP file clears the router secret before creating it; live-local scripts delete their rows | Database tests: own fixtures, clear leftovers |
| 2026-09-24 | gates.sh printed "GUARDS fail" but exited 0, and its test steps could not fail the run either, so f0cbb4a was pushed with failing guards | Failure paths | scripts/gates.sh exits non-zero on any failed step; scripts/guards/guard-gates-exit.mjs (proven against the pre-fix script) | Failure paths: gate scripts propagate failure |
| 2026-09-24 | Spans from turn-less jobs (fired cards, scheduled work) sat in the OTLP exporter's pending buffer forever - unbounded growth, never exported | Resource bounds | pending-eviction test in `otlp-turns.test.ts` | Failure paths: buffers waiting on a closing event are bounded |
| 2026-09-25 | The worker's SUPABASE_PROJECT_URL secret pointed at a stale project (neither of the Waldo org's two): the 00:28 exchange never reached togds (edge logs show zero calls in a window covering it, while a manual probe does appear), and turns kept "working" through the WALDO_OWNER_TELEGRAM_ID routing fallback, masking the broken integration | Ops: config drift | Live probes in PACKET_R2_2026-09-25.md: unsigned POST to the proxy must reach the function's own 401; step 1 re-pushes secrets from .env deterministically | Observability: a fallback that keeps the surface green hides a broken integration - verify the real path, not the fallback |
| 2026-09-25 | connector-proxy was deployed with the gateway JWT check on (the runbook's deploy line lacked --no-verify-jwt), while the worker authenticates with its own HMAC headers and no Authorization bearer - every call would 401 at the gateway even on the right project | Ops: deploy config | Probe criterion: an unsigned call must get the function's own `unsigned proxy call` 401, never the gateway's `Missing authorization header`; runbook line fixed | Trust boundaries: an edge function doing its own request auth deploys --no-verify-jwt, and the probe proves reachability |
| 2026-09-25 | P3 packet used a nonexistent `supabase db execute --sql` and queried `do_name, email` on waldo.connections (real columns: owner_id/provider/account/scopes/status), so the live verification step errored instead of verifying | Process: packet written from memory | `MAC_DEV_LOOP_2026-09-24.md` packet-writing rules: CLI invocations verified against the installed tool's help / current official docs, SQL identifiers copied verbatim from migration files | Packet hygiene: every CLI command and SQL identifier in a packet is verified against its live source before the packet ships |
| 2026-09-24 | OTLP export failures went only to the worker console, so a broken Langfuse path looked healthy from every owner-visible surface | Observability | `/langfuse` self-test command; export failures recorded in trace_log | Observability: exporter failures land in the trace book |
| 2026-09-24 | owner_wire_supabase.sh stage 3 generated a fresh router HMAC every run, stored it in Vault only when absent, but always pushed it to the worker - a re-run desynced worker vs Vault and broke every signed router call | Idempotency | scripts/guards/guard-owner-wire-hmac.mjs (proven against the pre-fix script) | Idempotency: read stored secrets back, generate only when absent |
| 2026-09-24 | db55804 put raw provider `output_items` into `LLMResponse`; the PostLLMCall scribe screened them under the `send_message` 4096 cap, so every live call failed "oversize" | Trust boundaries | `llm-provider.test.ts` output_items cases: over-4KB items pass verbatim, a canary in the text still halts, a canary-like string only in items does not; fix strips/restores `output_items` around PostLLMCall | Trust boundaries: raw provider passthrough fields never enter egress-destination sanitise candidates |
| 2026-09-24 | The W2/W3 waldo_* migrations landed without updating the canonical lists (`verify-supabase-migrations.mjs` and the SQL fixture `assert-canonical-migration-history.sql`), so every CI run failed on "migration list drifted" while local gates stayed green - the check was CI-only | Data and migrations | both canonical lists updated to the applied 15; gates.sh now runs the migration check locally | Data and migrations: adding a migration updates the canonical list in the same commit |
| 2026-09-24 | Owner asked "Give me google connector link"; no connect-intent tool existed (the consent URL was only a side effect of a failed calendar call) and no prompt rule covered setup asks, so the model improvised a made-up OAuth flow | Tool routing | `connect-service.test.ts` (URL surfaced, connected/no-config branches, strict args); contract test: connect_service granted exactly where query_calendar is, in the always-on set | Trust boundaries: connect/setup intent routes to the tool, never from memory |
| 2026-09-24 | Live consent proof failed: connect_service returned the signed consent URL inside the tool-result text, the model relayed it in its reply and rewrote it (signature truncated 43->8 chars, owner part altered), so Google's callback failed state validation and the failure page mislabeled every failure as expired | Trust boundaries | `connect-service.test.ts`: URL delivered via the button channel only, absent from model-visible text on every branch (deliver, no-deliver, deliver-failed, connected) | Trust boundaries: signed/secret-bearing URLs never pass through model-visible text; they travel in structured channels only |
| 2026-09-24 | After the button fix, query_calendar/draft_email (not-connected and 403 branches) and day cards still put the consent URL in model-visible text; the callback was stateless, so the live double-loaded callback (each hit twice, 2 s apart) would exchange the single-use code twice, a thrown proxy error gave a blank page, and every failure said "expired" | Trust boundaries, Idempotency | `google-consent.test.ts` (reload replays success with one exchange, thrown exchange renders failure, denied/expired/invalid distinct, tampered state never wakes the DO, attempt started mid-exchange survives, PKCE verifier sent); `google.test.ts`/`connections.test.ts` tool branches send a button and carry no URL (proven red on b9a4174) | Idempotency: OAuth callback settles once; Trust boundaries: the rule covers every link-minting path |
| 2026-09-25 | Every reply turn on the 57c098ce deploy died pre-flight: sanitiseRequest hard-failed the whole turn on any structural scribe deny (live trace: llm_reply error `forbidden (scribe_sanitise: invalid_payload)` at 0.00s, before OpenAI was ever called), so one un-sanitisable string in history/memory/system killed every turn; separately, zod v4 `toJSONSchema` stamps tool definitions with a non-configurable `~standard` marker holding function values - not JSON, riding the provider wire | Failure paths, Trust boundaries | `llm-provider.test.ts` structural-degradation cases (history reduced to the current message and re-sanitised, system prompt / tool turns dropped, a canary in history still fails closed, a dirty current message still fails closed); `llm.test.ts` toolParameters pure-JSON round-trip | Failure paths: structural sanitise denies degrade, hard security denies kill the turn |
| 2026-09-25 | Auth failures from external-origin tools (query_calendar, get_communication, ...) carried no source_taint stamp on the failure arm, so the dispatcher rejected them as invalid_handler_result - real 'not connected' errors surfaced live as handler bugs until S4 | Trust boundaries | handler.test.ts failure-arm taint cases; google.test.ts / connector-contract.test.ts stamp the failure arm 'external'; tool-loop.test.ts onConnect seam tests | Trust boundaries: external-origin tools stamp source_taint on success AND failure arms; auth failure is a typed intent, never text |
| 2026-09-25 | Every chat-issued set_reminder / list_reminders / cancel_reminder / propose_calendar_change / open_loop / close_loop / set_proactivity / browse_* call died at tool_arg_zod_validate with 'tool args schema unavailable' - those tools were registered with handlers but never entered TOOL_ARG_SCHEMAS in the hook registry, so the dispatcher halted them all and the model just claimed success anyway (L2-style mustCall checks and rubric judges cannot see this; only hop-outcome assertions can). Found by the first L1 scenario run. Separately pinned then root-caused and fixed same day: chat draft_email never executed, for THREE stacked reasons, each proven by the harness in sequence - (a) the PostLLMCall and PreToolUse scribe sanitisers redacted email addresses INSIDE executable tool args ([REDACTED_EMAIL] then failed zod validation; the pin's first diagnosis blaming the autonomy gate was wrong - the redaction halted it first), (b) draft_email sat in PRIVILEGED_ACTION_TOOLS while the chat path never wired hasApproval, and (c) the handler stamped its mutation-ack result taint 'external', which the dispatcher rejects for a non-external-origin tool. Fixes: executable tool args and PostLLMCall tool_calls are now checked (hard denies still halt) but never rewritten; draft_email demoted out of privileged (owner decision: drafts are reviewable artifacts, not sends); the draft result restamps source_taint null. | Failure paths | L1 scenario harness (`test/scenario-harness.test.ts` + `scenarios/catalog.ts`): tools-reminder and tools-propose-move assert ok:true tool hops (red before the fix), tools-draft-email asserts the draft hop succeeds end to end; hooks.test pins that executable args and tool_calls are never redacted, that a canary in tool-call arguments still halts fail-closed, and that send_message keeps its redaction pin | Failure paths: a tool is not live until a scenario proves its hop goes ok:true end to end |
| 2026-09-25 | owner_for_auth stamped used_by on EVERY used invite for the email with used_by null, not the invite just claimed - an old invite would be re-attributed to the newest owner | Data integrity | waldo_console_auth.sql regression: a pre-used invite for the same address keeps used_by null while only the claimed invite is stamped | Data and migrations: attribution writes name their exact row, never a re-queried set |
| 2026-09-25 | assert-canonical-migration-history.sql drifted again for 20260925000000/20260925010000 even after the 2026-09-24 drift rule: the mjs list check runs in sandbox gates but the SQL fixture only runs Mac-side, so fixture-only drift was invisible until a Mac run | Data and migrations | fixture brought to 19; scripts/guards/guard-migration-fixture-sync.mjs compares both lists statically in gates.sh (proven red against the stale fixture) | Data and migrations: both canonical lists stay in sync statically, checked where the work happens |
| 2026-09-25 | A directory-backed owner DO whose owner had not linked Telegram yet fell back to WALDO_OWNER_TELEGRAM_ID for its runtime identity - a second owner using the console before linking would have worn the first owner's Telegram identity, and the Google-connected confirmation would have gone to the first owner's chat | Multi-tenancy, Trust boundaries | owner-identity.test.ts pins resolveOwnerTelegramId: directory-backed + no subject resolves 0 (sends drop at the gate), linked subject wins, legacy env fallback kept only for non-directory deploys | Trust boundaries: an owner DO never borrows the deploy owner's identity; unlinked means sends drop, never reroute |
| 2026-09-25 | Console 'Connect Google' was a GET link that minted a connect-session row on every click - a link preview or refresh would stack junk sessions (same class as the spent-by-preview token bug) | Tokens | console.test.ts pins connect as a CSRF POST form and the absence of any GET /console/google link; GET now 303s back to the console | Tokens: anything that mints a session or spends a token rides a POST, never a GET |
| 2026-09-25 | c76f7cd added AI + Vectorize bindings at top level of wrangler.jsonc; vitest-pool-workers reads that file and starts a remote proxy session for them, so the entire runtime test pool began failing with a wrangler-auth error and a misleading 'no tests' summary | Failure paths | scripts/guards/guard-wrangler-local-bindings.mjs rejects top-level ai/vectorize bindings (proven red against the c76f7cd wrangler.jsonc) | Deploy and harness config: remote-only bindings live under env, never top-level |
| 2026-09-25 | Packet v3 step 1 ran `supabase test db` on the owner's Mac without rebuilding the local database first; the local DB lacked this week's migrations (console_session_open missing, whatsapp provider checks stale, grant contract stale) and carried leftover rows from a previous run (invite count 2-want-1, stale attribution) - 8 failures that were all machine state, zero repo. Same class as the 2026-09-25 fixture drift: Mac-side local state diverges and repo-side gates cannot see it | Data and migrations | durable fix is procedural by construction (machine state is invisible to repo gates): every packet step that runs pgTAP starts with `supabase db reset`; CI already starts from zero via `db start` | Database tests: pgTAP proves the repo only after the local DB is rebuilt from it |
| 2026-09-25 | waldo_console_sessions RPCs were created in 20260925010000 without revoking the PUBLIC default EXECUTE, so service_role and authenticated silently inherited EXECUTE on all five session functions; caught by the first-ever real run of the suite (fresh reset local DB) | Data and migrations | existing waldo_console_sessions.sql test 2 caught it live; fix migration 20260925120000 revokes PUBLIC on all five (applied migrations are immutable) | Data and migrations: a function-creating migration revokes PUBLIC EXECUTE in the same file |
| 2026-09-25 | waldo_whatsapp.sql throws_ok used the errname 'check_violation'; our pgTAP build never matches errnames, so correctly-thrown 23514 check violations reported as failures ('wanted: check_violation') | Database tests | same file re-pinned to SQLSTATE '23514' (2 assertions); the wrong assertions ARE the repro - they failed red against a correct constraint | Database tests: throws_ok pins the SQLSTATE, never the errname |
| 2026-09-25 | Three pgTAP failures shipped 'green' because the new waldo_console_sessions/waldo_whatsapp test files had never been EXECUTED - the sandbox has no local Supabase, and ship reports folded written-but-unrun tests into gate counts | Gates | process fix (no code can see it): ship reports now label unexecuted pgTAP UNRUN and first-run grading happens from Mac raw output | Database tests: only executed pgTAP counts as green; UNRUN is labeled |
