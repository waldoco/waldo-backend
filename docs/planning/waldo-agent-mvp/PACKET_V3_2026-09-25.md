# Packet v3 - Mac runbook (2026-09-25)

Run order matters. Repo: ~/Developer/Pin4sf/waldo-backend-mvp, branch beta-mvp.

## 0. Sync
```
cd ~/Developer/Pin4sf/waldo-backend-mvp && git pull --ff-only origin beta-mvp
git log -1 --format=%h   # expect the head I name when this packet is sent
```

## 1. Database (Supabase project togdshayyxycitzckpqv)
```
supabase db push                 # applies all pending migrations incl 20260925110000 whatsapp
supabase test db                 # pgTAP suite incl waldo_whatsapp.sql
```
Paste raw output of both.

## 2. Vectorize index for V1 (one-time create, then deploy picks up the binding)
```
cd packages/runtime
npx wrangler vectorize create waldo-recall --dimensions=768 --metric=cosine
```

## 3. Worker env updates (staging)
```
npx wrangler secret put LANGFUSE_CAPTURE_TEXT   # value: true        (trace previews on)
npx wrangler secret put WALDO_MCP_SERVERS       # value: []          (empty registry for now)
```
WALDO_TOOL_OFFLOAD: leave unset for now (read_tool_output stays off until the offload path
gets its live check; noted honestly).

## 4. Deploy
```
npx wrangler deploy --env staging
```

## 5. Live checks (paste raw output of each)
```
curl -s https://waldo-runtime-staging.piyushfulper3210.workers.dev/healthz
```
Then in Telegram: send Waldo one message ("trace check") and open the newest Langfuse trace:
confirm the preview shows your message + reply and the tree nests pickup/llm/tool_*/send.
Screenshot or describe what you see.

## 6. Console sign-in (A3 live proof)
Open the console URL, sign in with the email code, paste any error verbatim.
